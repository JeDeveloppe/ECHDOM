<nav class="navbar navbar-expand-lg fixed-top py-3 zen-navbar">
        <div class="container">
            <a class="navbar-brand fw-bold text-gold" href="/">WORKaway</a>
            <button class="navbar-toggler border-gold" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav mx-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link link-nav fw-semibold text-gold" href="/">
                            <i class="fas fa-arrow-left me-2"></i>Retourner au site
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="{{ path('member_dashboard') }}">Tableau de bord</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="{{ path('member_properties') }}">Mes propriétés</a>
                    </li>
                </ul>
                <div class="d-flex gap-2 ms-auto align-items-center">
                    <div class="dropdown">
                        <a href="#" class="d-flex align-items-center text-decoration-none dropdown-toggle" id="userMenu" data-bs-toggle="dropdown" aria-expanded="false">
                            <img src="{{ asset(app.user.avatarUrl|default('/images/' ~ app.user.gender.assetsLink)) }}" alt="Avatar" class="rounded-circle me-2" style="width:36px;height:36px;">
                            <span class="fw-semibold">Bonjour {{ app.user.firstName|default(app.user.email) }}</span>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end shadow" aria-labelledby="userMenu">
                            <li><a class="dropdown-item" href="{{ path('member_dashboard') }}"><i class="fas fa-user me-2"></i> Mon tableau de bord</a></li>
                            <li><a class="dropdown-item" href="#"><i class="fas fa-sync-alt me-2"></i> Mes échanges</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li>
                                <a class="dropdown-item text-danger" href="{{ path('app_logout') }}">
                                    <i class="fas fa-sign-out-alt me-2"></i> Déconnexion
                                </a>
                            </li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </nav>